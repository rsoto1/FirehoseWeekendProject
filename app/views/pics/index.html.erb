<div class="hero-unit">
	<h2>Here are all the pics!</h2>

	<% @pics.each do |pic| %>
		Emotion: <%= pic.emotion %>
		<p />
		Learned: <%= pic.learned %>
		<p />
		Hour: <%= pic.hour %>
	<% end %>
	<br />
</div>

<%= link_to 'New Pic', new_pic_path %>